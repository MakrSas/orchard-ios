<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Lets a debugger attach, which is how StikDebug (or Xcode) turns on
         JIT: without it PT_TRACE_ME and MAP_JIT are both refused. Copied
         from ../app/Resources/entitlements.plist — same mechanism, see
         JIT.swift. -->
    <key>get-task-allow</key>
    <true/>
</dict>
</plist>
